>npm init
>npm install express morgan cors dotenv mysql2 bcrypt nunjucks chokidar

//github repository 에는 node_modules는 올라가지 않는다.
//팀원이 clone하면 package.json만 공유된다.
//npm install
//package.json에 명시된 모듈들이 다운로드 된다 => 팀장과 동일 환경.
-----------------------------------------------------------------------서버 껍데기(back-end)
> npx nodemon app.js
http://localhost:8000/

//개별 github repository에 push
>git add .
>git commit -m "초기 셋팅"
>git push -u origin main

//지운 파일 복원시키기
1. 첫번째 방법
>git log --oneline
>git reset --hard 해시
    (해시 => 돌리고자 하는 부분의 해시값)
>git push  -f origin main 
    (동기화가 안되어있어도 강제로 push)
2. 두번째 방법 (새로운 폴더 및 환경 생성)


//서버 구동 명령어
>npm nodemon app.js

//프론트 구동 명령어
>npm run dev