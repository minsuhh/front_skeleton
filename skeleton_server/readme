>npm init
>npm install express morgan cors dotenv mysql2 bcrypt nunjucks chokidar

//github repository 에는 node_modules는 올라가지 않는다.
//팀원이 clone하면 package.json만 공유된다.
//npm install
//package.json에 명시된 모듈들이 다운로드 된다 => 팀장과 동일 환경.
-----------------------------------------------------------------------서버 껍데기(back-end)
> npx nodemon app.js
http://localhost:8000/

//개별 github repository에 push
>git add .
>git commit -m "초기 셋팅"
>git push -u origin main